import { ImageObject } from '../../interfaces';
import { MMSImage } from './MMSImage';
export declare class Image extends MMSImage {
    constructor(image: ImageObject, to?: string, from?: string, clientRef?: string);
}
