import { AudioObject } from '../../interfaces';
import { MMSAudio } from './MMSAudio';
export declare class Audio extends MMSAudio {
    constructor(audio: AudioObject, to?: string, from?: string, clientRef?: string);
}
